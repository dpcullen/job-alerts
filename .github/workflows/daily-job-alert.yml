name: Daily Job Alert
# v2

on:
  schedule:
    # 8:00 AM EST (UTC-5) = 13:00 UTC
    # Note: shifts to 9 AM during summer daylight saving time (EDT = UTC-4)
    - cron: '0 13 * * *'
  workflow_dispatch:  # Allows manual "Run workflow" from the GitHub UI

concurrency:
  group: job-alert
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  send-alert:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run job alert
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          TO_EMAIL: ${{ secrets.TO_EMAIL }}
        run: python job_alert.py

      - name: Save seen jobs back to repo
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add seen_jobs.json
          git diff --staged --quiet || git commit -m "chore: update seen jobs [skip ci]"
          git push
